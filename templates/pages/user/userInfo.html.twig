{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
<div style="padding-top:90px;"></div>
<h1 class="text-center mt-4 mb-5 align-self-center" id="titreUs">Bonjour {{app.user.prenom}}</span></h1>

<main class='paddingTop'>
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">Profile</h5>
    <h6 class="card-subtitle mb-2 text-body-secondary">{{app.user.prenom}} {{app.user.nom}}</h6>
    <h6 class="card-subtitle mb-2 text-body-secondary">{{app.user.email}}</h6>
    <h6 class="card-subtitle mb-2 text-body-secondary">{{app.user.numTel}}</h6>
    <a href="{{path('app_user_edit', {id: app.user.id})}}" class="card-link">Manage</a>
  </div>
</div>

<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">Adresse Book</h5>
    <h6 class="card-subtitle mb-2 text-body-secondary">{{app.user.numMaison}} {{app.user.nomVoie}}</h6>
    <h6 class="card-subtitle mb-2 text-body-secondary">{{app.user.ville}}</h6>
    <h6 class="card-subtitle mb-2 text-body-secondary">{{app.user.codePostale}}</h6>
    <a href="{{path('app_edit_adresse', {id: app.user.id})}}" class="card-link">Manage</a>
  </div>
</div>
    {# {% if app.user %}
<h1 class="text-center mt-4 mb-5 align-self-center" id="titreUs">Bonjour {{app.user.prenom}} {{app.user.nom}}</span></h1>

<div style=" display: flex; justify-content: center;">
<div class="card mt-5" id='carte' style="width: 30rem;">
<div class="card-body">
    <h5 class="card-title text-center text-primary">Mon Profil</h5>
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item text-center text-primary" >Nom : <span class="text-secondary">{{app.user.nom}} {{app.user.prenom}}</span></li>
    <li class="list-group-item text-center text-primary" >Email : <span class="text-secondary">{{app.user.email}}</span></li>
    <li class="list-group-item text-center text-primary" >Téléphone : <span class="text-secondary">{{app.user.numTel}}</span></li> #}
    {# <li class="list-group-item text-center text-primary" >Adresse : <span class="text-secondary">{{ app.user.numMaison }} {{ app.user.nomVoie }}<br>{{ app.user.ville }}  {{ app.user.codePostal }}</span></li> #}
    {# <li class="list-group-item text-center text-primary" >Livraison Favorite : <span class="text-secondary">{{app.user.livraisonFav}}</span></li> #}
    {# <li class="list-group-item text-center text-primary"><a class="btn btn-primary btn-lgz" href="/user/edition/{{app.user.id}}" role="button">Modifier mon profil</a></li>
    <li class="list-group-item text-center text-primary"><a class="btn btn-primary btn-lgz" href="/user/mdp/{{app.user.id}}" role="button">Modifier mon mot de passe</a></li>
    <li class="list-group-item text-center text-primary"><a class="btn btn-danger btn-lgz" href="/user/{{app.user.id}}" role="button">Supprimer mon compte</a></li>
  </ul>
</div>
</div>
    {% else %}
    <div class="d-flex justify-content-evenly">
      <div class="d-flex flex-column">
        <p class="align-self-center">Si tu possède un compte , connecte toi</p>
          <a href="{{path("app_login")}}" class="btn btn-primary mt-4">Se connecter</a>
      </div>
      <div class="d-flex flex-column">
        <p class="align-self-center">Tu ne possède pas de compte, inscrit toi</p>
          <a href="{{path("app_user_register")}}" class="btn btn-secondary mt-4">S 'inscrire</a>
      </div>
    </div>
    {% endif %} #}

    </main>
{% endblock %}